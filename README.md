# 我的书房

一个可直接部署到 GitHub Pages 的静态个人博客。

## 添加文章

编辑 `posts.js` 中的数组。每篇文章支持标题、分类、日期、摘要、正文，以及图片或视频地址。

## 本地预览

直接打开 `index.html`，或在本目录运行任意静态文件服务器。

## 部署

仓库包含 GitHub Pages 工作流。推送到 `main` 分支后，在仓库的 **Settings → Pages** 中将 Source 设为 **GitHub Actions**。
